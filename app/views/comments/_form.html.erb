<%=form_for ([@article,@article.comments.build]) do |f|  %>
    <div class="form-group">
      <%= f.label :commenter %>
      <%= f.text_field :commenter %>
    </div>

    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_field :body %>
    </div>

    <div class="form-group">
      <%= f.submit%>
    </div>


<% end %>